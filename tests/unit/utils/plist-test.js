import plist from 'fontinstaller/utils/plist';
import { module, test } from 'qunit';

module('Unit | Utility | plist', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = plist();
    assert.ok(result);
  });
});
